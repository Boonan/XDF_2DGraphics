Our group has found a library to import SVG file and draw it on the screen, 
but xml language is too big to save and occupying much memory will lead procedure slow.
Therefore, we would like to improve our code and compact xml.
Find a library to extract shape information of SVG file. Transform SVG file and save shape information into JSON file. 
Create shape class to draw JSON file. 
//TODO: find a library to match shape information in the JSON file. Draw shape information in the JSON file. create more class to save and draw shape information.
